
import logo from '../assets/radpal_logo2.png'
import { useState } from 'react'

export default function Header({ userEmail, onLogout, isContracted, tokenUsage }) {
  return (
    <header style={{ margin: 0, padding: 0, lineHeight: 1, position: 'relative' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 12, justifyContent: 'center' }}>
        
      </div>
    </header>
  )
}
